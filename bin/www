#!/usr/bin/env node
'use strict'

const app = require('../lib/app')

app.listen(app.get('settings').port, () => {
  app.get('logger').info('Server has started', {
    port: app.get('settings').port
  })
})
