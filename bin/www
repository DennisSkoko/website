#!/usr/bin/env node

'use strict';

var app = require('../lib/app');

app.listen(app.get('settings').port, function() {
    console.log('Server is listening to port: ' + app.get('settings').port);
});
