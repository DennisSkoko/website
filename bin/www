#!/usr/bin/env node
'use strict'

const app = require('../lib/app')
const http = require('http')

const server = http.createServer(app)

server.listen(app.get('settings').port, () => {
  app.get('logger').info('Server has started', {
    port: app.get('settings').port
  })
})
